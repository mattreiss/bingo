import * as React from 'react';
import * as ReactDOM from 'react-dom';
import BingoGame from './components/BingoGame';

function render() {
  ReactDOM.render(<BingoGame />, document.body);
}

render();